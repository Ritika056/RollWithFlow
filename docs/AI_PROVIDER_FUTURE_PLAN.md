# AI Provider Future Plan

`AI_PROVIDER`, `AI_API_KEY`, and `AI_MODEL` are optional backend settings. Without a configured provider, RollWithFlow remains in local mock mode and ranks the local database using likes, playable sources, metadata, notes, genres, ratings, BPM, and energy. No external AI request is made by default.
