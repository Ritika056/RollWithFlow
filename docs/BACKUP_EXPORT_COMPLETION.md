# Backup and Export

Settings exports Library CSV/JSON, Playlists JSON, and a full JSON backup without audio files. Backup JSON can be validated before any future restore workflow. Restore is deliberately not enabled yet.
