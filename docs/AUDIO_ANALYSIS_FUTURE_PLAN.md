# Audio Analysis Future Plan

Current local analysis remains lightweight and metadata-safe. A future phase can add an offline BPM/key engine, waveform generation, cue-point detection, beat grids, and background-job processing. It must never invent BPM or key values when detection is unavailable.
